[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "vlmodels"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["PyOMES", "numpy", "scipy"]

[tool.pytest.ini_options]
testpaths = ["../tests/standalone"]
